# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Filesystem
        - PhpBrowser:
            url: fileshare.me/
        - \Helper\Acceptance
        - \Helper\Functional
        - Db:
            dsn: 'mysql:host=localhost;dbname=fileshare'
            user: 'honey'
            password: 'd5d7'
            dump: 'dump.sql'

            cleanup: true
            reconnect: true
