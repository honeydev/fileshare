<!--files review -->
<!-- Comment modal window -->

<div class="container">
    <div class="row">
        <aside class="settingsMenu col-xs-offset-2 col-xs-10 col-sm-offset-4">
            <div class="btn-group">
                <a href="/browse/late_to_early">
                    <button type="button " class="btn btn-default lateToEarlyButton
                        {% if sortType == 'late_to_early' %}
                            active
                        {% endif %}
                        ">Late to early
                    </button>
                </a>
                <a href="/browse/early_to_late">
                    <button type="button" class="
                    btn btn-default
                    {% if sortType == 'early_to_late' %}
                        active
                    {% endif %}
                    ">Early to late
                    </button>
                </a>
            </div>
        </aside>

        <section class="files col-xs-10">
            {% for fileArticle in fileArticles %}
                <article class="file">
                    <a href="{{ fileArticle.file.filePageUrl }}">
                    <h4><img src="{{ fileArticle.owner.avatarUri }}" alt="Author avatar" class="img-rounded userAvatar">
                        {{ fileArticle.owner.email }}
                    </h4>
                    <h4>Filename {{ fileArticle.file.name }}</h4>

                    <img 
                    class="
                    img-responsive center-block
                    {% if fileArticle.file.fileAvatar.type == 'default' %} 
                        fileImageDefault
                    {% elseif fileArticle.file.fileAvatar.type == 'supported' %}
                        fileImageSupported
                    {% endif %}
                    "
                    src="
                    {{ fileArticle.file.fileAvatar.url }}
                    " 
                    alt="File type pic"
                    >
                    <ul class="fileInfoList list-inline">
                        <li>Uploaded: {{ fileArticle.file.created }}</li>
                        <li>Size: {{ fileArticle.file.size }} Mb</li>
                    </ul>
                    </a>
                </article>
            {% endfor %}
        </section>
        {% include 'file_navigation.twig' %}
    </div>
</div>
